/**
 * Adds two numbers.
 * @param {int} a - Accepts number input.
 * @param {int} b - Accepts number input.
 * @return {int} Returns number output.
 */
function sum(a, b) {
  return a + b;
}

export default sum;
